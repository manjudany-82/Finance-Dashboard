# Example environment file for Finance Dashboard
# Copy this to .env and fill in your own secrets. Do NOT commit your .env file.

# Gemini (Google) API key for generative models
# Example: GEMINI_API_KEY=ya29.A0ARrdaM...   (use your key or service account creds)
GEMINI_API_KEY=

# Optional: list of models (comma-separated) to try, e.g. "gemini-1.5-flash,gemini-2.5-flash"
AI_CANDIDATES=gemini-1.5-flash,gemini-2.5-flash,gemini-2.5-pro

# Preferred single model to try first (recommended for lower quota pressure)
# Example: GEMINI_PREFERRED_MODEL=gemini-1.5-flash
GEMINI_PREFERRED_MODEL=gemini-1.5-flash

# Other environment variables
# DEBUG=true
AZURE_CLIENT_ID=your_client_id_here
AZURE_CLIENT_SECRET=your_client_secret_here
AZURE_TENANT_ID=your_tenant_id_here
GEMINI_API_KEY=your_gemini_api_key_here
